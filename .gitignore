# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo
# Keep Next.js ambient types file tracked for editor stability
# (Next.js regenerates it if missing; do not edit its contents)
# next-env.d.ts NOT ignored intentionally

# IDE/editor artifacts
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea/
.fleet/

# OS / file system cruft
Thumbs.db
*.swp
*.swo
.DS_Store

# Logs (generic catch-all)
*.log
logs/

# Coverage & instrumentation
.nyc_output/
coverage-final.json

# Node / build caches
.turbo
.eslintcache
.swc/

# Environment & secrets (keep examples via *.example)
.env.local
.env.development
.env.test
.env.production
.envrc
.npmrc
!.env.example

# Databases / local storage
*.sqlite*
*.db
*.db-journal

# Misc binaries & archives
*.tgz
*.tar
*.gz
*.zip
*.bak
*.orig

# Yarn berry cache (already covered, redundancy acceptable)
.yarn/cache

# Serverless / deployment artifacts (future-proof)
.serverless/
.dist/
dist/

# Ignore generated type augmentations from tools (keep explicit ones in src/ if needed)
types/*/*.generated.d.ts

docs/
